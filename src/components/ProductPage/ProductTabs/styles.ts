import styled from 'styled-components'

export const ContainerDetailsProduct = styled.div`
  width: 100%;
  max-width: 1024px;
  margin: ${({ theme }) => theme.spacing.x9} auto;
`

export const TabContent = styled.div`
  width: 100%;
  padding: ${({ theme }) => theme.spacing.x4};
  overflow: hidden;
`
